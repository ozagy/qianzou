<h2>New photo</h2>
<% @photo.album_id = @album if @album %>

<%= render 'form' %>

<% if @album %>
  <%= link_to 'Back', album_path( @album ), :class => 'btn' %>
<% else %>
  <%= link_to 'Back', photos_path, :class => 'btn' %>
<% end %>
